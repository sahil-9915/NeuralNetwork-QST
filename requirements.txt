# requirements.txt


numpy>=1.20.0
matplotlib>=3.5.0
qutip>=4.7.0
ipython>=8.0.0
scipy>=1.8.0  # For numpy.linalg.pinv (comes with numpy, but explicitly for completeness)

# Optional but recommended
jupyter>=1.0.0  # If you're using Jupyter notebooks
seaborn>=0.12.0  # For the seaborn-v0_8-darkgrid style (fallback matplotlib style)
